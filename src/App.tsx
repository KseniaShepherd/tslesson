import React, { FC, useState } from 'react';
import { useSelector } from 'react-redux';
import { useAppSelector } from './hooks/redux';


function App() {
  return (
    <>
      {/* <User
        variant={IUserVariant.red}
        username={user.username}
        age={user.age}
        onClickProps={() => {
          console.log('hi');
        }}
      /> */}
    </>
  );
}

export default App;
